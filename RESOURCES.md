# Pixel Art Tutorials:
https://saint11.art/blog/pixel-art-tutorials/

# Line count command:
find . -type f \( -name "*.c" -o -name "*.h" -o -name "*.glsl" -o -name "*.vs" -o -name "*.fs" \) -not -path "./src/third_party/*" -not -path "./src/shaderTest/*" | xargs wc -l

# Tool for normal maps:
https://azagaya.itch.io/laigter
